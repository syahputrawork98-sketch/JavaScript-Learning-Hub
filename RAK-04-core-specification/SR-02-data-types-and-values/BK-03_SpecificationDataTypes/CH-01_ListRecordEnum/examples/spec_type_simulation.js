/**
 * LAB: Specification Data Structures Simulation
 */
const COMP_TYPE = Object.freeze({ NORMAL: Symbol("normal") });
console.log(`Spec Enum: ${COMP_TYPE.NORMAL.description}`);
const argList = ["JS", "Expert"];
console.log(`Spec List length: ${argList.length}`);
