// C12 - Example 02: reproduksi minimal bug

const rawQty = '';
const total = Number(rawQty) + 5;

console.log('rawQty:', rawQty, '| typeof:', typeof rawQty);
console.log('Number(rawQty):', Number(rawQty));
console.log('total:', total);
